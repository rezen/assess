'use strict';

function C(B) {
  return B;
}

class Hey extends    
 C(Object) {

  job() {
    function catDog (cats = [1, 3], dogs = {a: 'b', c: 'd'}) {
      if (cats.length) {
        return 'z';
      }

      return dogs.c;
    }

    return 12;
  }

  jas() {

  }

  constructor    ( greeting = {a: 'b', c: 'd'  }, callback=C(1) ) {
    function catDog (cats = [1, 3], dogs = {a: 'b', c: 'd'}) {
      if (cats.length) {
        return 'z';
      }

      return dogs.c;
    }

    if (cats.length > 0)
      return false
  }


  static hey(){}

  static meow(zing){ console.log(',m')

  return console.log(99);
}
  
  /*
  // @todo breaks stuff
  bob(n= 12) 
  {
    n++;
  }
  */
 
 bob(n= 12) {
    n++;
  }

  jon(callback) {return 'false';}
}

Hey.laha = 777;

module.exports = Hey;
